import { StyleSheet } from 'react-native';

const OrderMedicationStyles = StyleSheet.create({
    container: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      backgroundColor: '#ffffff',
    },
    title: {
      fontSize: 24,
      marginBottom: 20,
    },
  });

export default OrderMedicationStyles;